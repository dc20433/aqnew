<nav class="navbar navbar-expand-lg navbar-dark bg-secondary">
  <div class="container-fluid">
    <%= link_to "Home", root_path, class:"navbar-brand" %>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <ul class="navbar-nav me-auto mb-2 mb-lg-0">
        <li class="nav-item">
        <li class="nav-item dropdown">
          <a class="nav-link dropdown-toggle active" href="#" role="button" data-bs-toggle="dropdown" aria-expanded="false">
            Site Pages
          </a>
          <ul class="dropdown-menu">
            <li><a class="dropdown-item" href="#">Insurance Form</a></li>
            <li><%= link_to "Consent Form", consent_path, class:"dropdown-item" %></li>
            <li><hr class="dropdown-divider"></li>
            <li class="nav-item">
              <% if user_signed_in? %>
                <%= link_to "Patients", regis_path, class:"dropdown-item" %>
              <% end %>
            </li>
            <li class="nav-item">
              <% if user_signed_in? %>
                <%= link_to "Users", users_path, class:"dropdown-item" %>
              <% end %>
            </li>
            
          </ul>
        </li>
          <%# if current_user %>
            <%# link_to "<< Current User: #{current_user.name} >>", user_path(current_user), class:"nav-link active" %>
          <%# end %>
        </li>  
        <li class="nav-item">
          <% if user_signed_in? %>
            <%= link_to "Log out", destroy_user_session_path, class:"nav-link active", data: { turbo_method: :delete } %>
          <% else %>
        </li>
        <li class="nav-item">
          <%= link_to "Log in", new_user_session_path, class:"nav-link active" %>
        </li>
        <li class="nav-item">
          <%= link_to "Sign up", new_user_registration_path, class:"nav-link active" %>
        </li>
          <% end %>
        
      </ul>
      <li class="d-flex">
        <%# if user_signed_in? %>
          <p class="text-white">User.(params[:id]).email</p>
          <%# link_to "<< Current User: #{current_user.email} >>", user_path(current_user), class:"nav-link active" %>
        <%# end %>
      <li>
    </div>
  </div>
</nav>