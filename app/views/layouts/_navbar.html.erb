<nav class="navbar navbar-expand navbar-dark bg-primary pt-1 pb-1">
  <div class="container-fluid">
    <%= link_to "Home", root_path, class:"navbar-brand" %>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <ul class="navbar-nav me-auto mb-2 mb-lg-0">
        
        <%= link_to "Insurance", "#", class:"nav-link active" %>
        <%= link_to "Consent Form", consent_path, class:"nav-link active" %>

        <li class="nav-item">
            <% if current_user %>
              <%= link_to "Patients", regis_path, class:"nav-link active" %>
            <% end %>
            </li>
        <li class="nav-item">
          <% if current_user %>
            <%= link_to "Users", users_path, class:"nav-link active" %>
          <% end %>
        </li> 

        <% if current_user %>
          <li class="nav-item">
            <%= link_to "Log out", destroy_user_session_path, class:"nav-link active", data: { turbo_method: :delete } %>
          </li>
        <% else %>
          <li class="nav-item">
            <%= link_to "Log in", new_user_session_path, class:"nav-link active" %>
          </li>
          <li class="nav-item">
            <%= link_to "Sign up", new_user_registration_path, class:"nav-link active" %>
          </li>
        <% end %>  
      </ul>

      <li class="d-flex">
        <% if current_user %>
          <p class="text-white bg-dark">Current User Name: <%= current_user.name %> | Email: <%=current_user.email %></p>
        <% end %>
      </li>
    </div>
  </div>
</nav>